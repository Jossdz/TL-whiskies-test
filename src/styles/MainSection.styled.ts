import styled from "styled-components"

export default styled.section`
  margin-top: 3.8rem;
  padding: 6rem 12%;
  width: 100vw;
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(min(420px, 640px), 1fr));
  grid-template-rows: auto;
  gap: 80px;
`
